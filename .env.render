# Environment Variables for Render Deployment
# Copy these to Render Dashboard → Your Service → Environment

# Required - Change this to a strong secret key
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-use-random-string

# Optional - Google OAuth (if you want Google login)
# Get these from https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id-from-google-cloud-console
GOOGLE_CLIENT_SECRET=your-google-client-secret-from-google-cloud-console
GOOGLE_CALLBACK_URL=https://rewear-ahp2.onrender.com/api/auth/google/callback

# Note: These are already set in the Docker container, no need to add:
# - NODE_ENV=production
# - PORT=5000
# - DATABASE_URL (PostgreSQL is included in container)
# - UPLOAD_PATH=/uploads
# - CORS_ORIGIN=*
# - MAX_FILE_SIZE=5242880
